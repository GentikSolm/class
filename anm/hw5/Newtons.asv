function estimate = Newtons(fx, fpx, tol, x0)
%NEWTONS Summary of this function goes here
%   Determins an approximation to the root given fx, fpx, a tolorant and x0
    x = x0;
    while abs(fx(x)) > tol
    end
end

